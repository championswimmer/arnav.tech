---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Content, frontmatter } from '../content/about.md';
---

<BaseLayout title={`${frontmatter.title} · Arnav Gupta`} description={frontmatter.description}>
	<article class="reading-essay">
		<h1>{frontmatter.title}</h1>
		<Content />
	</article>
</BaseLayout>
